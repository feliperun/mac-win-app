name: validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  powershell:
    name: PowerShell syntax
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse all .ps1 files
        shell: pwsh
        run: |
          $errors = @()
          Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
            $tokens = $null
            $parseErrors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName, [ref]$tokens, [ref]$parseErrors
            ) | Out-Null
            if ($parseErrors.Count -gt 0) {
              Write-Host "ERROR: $($_.FullName)" -ForegroundColor Red
              $parseErrors | ForEach-Object { Write-Host "  $($_.Message)" }
              $errors += $_.FullName
            } else {
              Write-Host "OK:    $($_.FullName)" -ForegroundColor Green
            }
          }
          if ($errors.Count -gt 0) { exit 1 }

  autohotkey:
    name: AutoHotkey syntax
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install AutoHotkey v2
        shell: pwsh
        run: |
          winget install --id AutoHotkey.AutoHotkey `
            --accept-source-agreements `
            --accept-package-agreements `
            --silent `
            --scope user
      - name: Validate .ahk files
        shell: pwsh
        run: |
          $ahk = Get-ChildItem -Recurse -Path "$env:LOCALAPPDATA\Programs\AutoHotkey" -Filter "AutoHotkey64.exe" | Select-Object -First 1
          if (-not $ahk) { throw "AutoHotkey not found after install" }
          Get-ChildItem -Recurse -Filter *.ahk | ForEach-Object {
            Write-Host "Validating $($_.FullName)"
            & $ahk.FullName /validate $_.FullName
            if ($LASTEXITCODE -ne 0) { throw "AHK validation failed on $($_.FullName)" }
          }
