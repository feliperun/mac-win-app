name: validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  powershell:
    name: PowerShell syntax
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse all .ps1 files
        shell: pwsh
        run: |
          $errors = @()
          Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
            $tokens = $null
            $parseErrors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName, [ref]$tokens, [ref]$parseErrors
            ) | Out-Null
            if ($parseErrors.Count -gt 0) {
              Write-Host "ERROR: $($_.FullName)" -ForegroundColor Red
              $parseErrors | ForEach-Object { Write-Host "  $($_.Message)" }
              $errors += $_.FullName
            } else {
              Write-Host "OK:    $($_.FullName)" -ForegroundColor Green
            }
          }
          if ($errors.Count -gt 0) { exit 1 }

  autohotkey:
    name: AutoHotkey syntax
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install AutoHotkey v2
        shell: pwsh
        run: |
          $tag = "v2.0.23"
          $url = "https://github.com/AutoHotkey/AutoHotkey/releases/download/$tag/AutoHotkey_2.0.23.zip"
          $zip = "$env:RUNNER_TEMP\ahk.zip"
          $dst = "$env:RUNNER_TEMP\ahk"
          Write-Host "Downloading $url"
          Invoke-WebRequest -Uri $url -OutFile $zip -UseBasicParsing
          Expand-Archive -Path $zip -DestinationPath $dst -Force
          $exe = Get-ChildItem -Recurse -Path $dst -Filter "AutoHotkey64.exe" | Select-Object -First 1
          if (-not $exe) { throw "AutoHotkey64.exe not found in zip" }
          "AHK_EXE=$($exe.FullName)" | Out-File -Append -FilePath $env:GITHUB_ENV -Encoding utf8
          Write-Host "AHK: $($exe.FullName)"
      - name: Validate .ahk files
        shell: pwsh
        run: |
          $failed = @()
          Get-ChildItem -Recurse -Filter *.ahk | ForEach-Object {
            Write-Host "Validating $($_.FullName)"
            $stdout = & $env:AHK_EXE /validate $_.FullName 2>&1 | Out-String
            $code   = $LASTEXITCODE
            Write-Host "  exit code: $code"
            if ($stdout.Trim()) { Write-Host "  output: $stdout" }
            # AHK /validate: exit 0 = OK, exit 2 = syntax error.
            # Exit 1 sometimes happens for warnings on headless runners; treat only 2+ as failure.
            if ($code -ge 2) {
              $failed += $_.FullName
            } else {
              Write-Host "  OK"
            }
          }
          if ($failed.Count -gt 0) {
            throw "AHK validation failed on: $($failed -join ', ')"
          }
